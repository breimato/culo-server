post:
  summary: "Post Room Close V1"
  description: "Close the room for all players — host only (STOMP /app/room.close)"
  operationId: "PostRoomCloseV1"
  tags:
    - post-room-close-v1
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../requests/PostRoomCloseV1Request.yaml'
  responses:
    '204':
      description: "204 No Content"
    '500':
      description: "Internal Server Error"
      content:
        application/json:
          schema:
            $ref: '../components/ApiErrorV1.yaml'
