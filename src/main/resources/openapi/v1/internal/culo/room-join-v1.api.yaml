post:
  summary: "Post Room Join V1"
  description: "Join an existing room (STOMP /app/room.join)"
  operationId: "PostRoomJoinV1"
  tags:
    - post-room-join-v1
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../requests/PostRoomJoinV1Request.yaml'
  responses:
    '204':
      description: "204 No Content"
    '500':
      description: "Internal Server Error"
      content:
        application/json:
          schema:
            $ref: '../components/ApiErrorV1.yaml'
