post:
  summary: "Post Room Start V1"
  description: "Start the game in a room (STOMP /app/room.start)"
  operationId: "PostRoomStartV1"
  tags:
    - post-room-start-v1
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../requests/PostRoomStartV1Request.yaml'
  responses:
    '204':
      description: "204 No Content"
    '500':
      description: "Internal Server Error"
      content:
        application/json:
          schema:
            $ref: '../components/ApiErrorV1.yaml'
